# Chat

OpenAI API endpoint reference.

Canonical reference URL: https://developers.openai.com/api/reference/resources/chat

This API reference page is generated by Stainless.

Local full-page capture: `docs/research/openai/chat_completions_reference_2026-04-12.html`

Agent implementation notes used on 2026-04-12:

- Chat Completions requests support output token caps through `max_completion_tokens`; this repo keeps `max_tokens` only as a legacy/provider-compat request field.
- Streaming usage accounting is provider surfaced through Chat Completions `usage`; repo fallback estimates are explicitly marked `estimated:true` and do not pretend to be provider-token-accurate.
- `stream_options.include_usage` remains best-effort across OpenAI-compatible providers, so compatibility retry paths must preserve streaming behavior when a provider rejects that field.
