openapi: 3.0.3
info:
  title: broker HTTP API
  version: "v1"
  description: |
    Machine-readable contract for the broker relay service.

    Notes:
    - Authentication/authorization is typically via OIDC (see docs/BROKER.md).
    - Some endpoints are upgraded to WebSocket or stream via SSE.
servers:
  - url: http://127.0.0.1:8081

tags:
  - name: health
  - name: caps
  - name: client_prefs
  - name: client_auth
  - name: agents
  - name: audio
  - name: memory
  - name: membership
  - name: teams
  - name: orchestrate
  - name: events

paths:
  $ref: ./broker/paths.yaml

components:
  $ref: ./broker/components.yaml
